#Tuples are immutable
#we can store multiple datatypes in a tuple
#we cann't write a single value as a tuple 
#major diff: b/w list and tuple is (lists=>Mutable,tuple=>immutable)
tup = (1,2,3,4,5,6)
print (tup)
print (type(tup))
print (len(tup)) 