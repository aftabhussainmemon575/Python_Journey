#set is a collection of elements.
#elements must be unique and immutable.
#sets are mutable and unordered.
set = {1,2,2,2,3,4}     #repeated nums are ignored
print(set)
print (len(set))
print (type(set))