'''1. Built-in Functions
These are functions that are already pre-defined in Python. You don’t need to import any modules or write any definition code to use them; they are always available out of the box.

Examples: print(), len(), type(), input(), max(), min(), and sum()'''


'''2. User-Defined Functions
As the name suggests, these are functions created by programmers to perform specific tasks tailored to their application. You define them using the def keyword.

Characteristics: They can take positional or keyword arguments, have default parameters, and optionally return values using the return keyword.'''

